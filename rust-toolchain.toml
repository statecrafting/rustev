# The toolchain CI resolved from `stable` on 2026-09-25 (rustc 1.98.1),
# pinned so a local build and CI judge the same code with the same lints.
# Move it deliberately, in its own change.
[toolchain]
channel = "1.98.1"
components = ["clippy", "rustfmt"]
profile = "minimal"
